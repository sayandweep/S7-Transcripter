version: "3.9"

services:
  api:
    build: .
    container_name: transship-api
    restart: unless-stopped

    ports:
      - "5000:5000"

    env_file:
      - .env

    volumes:
      - ./uploads:/app/uploads
      - ./temp:/app/temp